<div class="logo">
  <img srcset="{{data.logo}} 1x, {{data.logo}} 1.5x, {{data.logo}} 2x"
    src={{data.logo}} width="100" height="100" alt="bot image">
</div>

<div class="name">
  {{data.name}}
</div>

<div class="social-networks-connected">
  <div>
    <label for="telegram">telegram</label>
    <input type="checkbox" name="telegram" id="telegram" checked={{data.telegram}}>
  </div>
</div>

{{yield}}